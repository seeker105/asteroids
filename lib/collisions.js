// const Asteroid = require('../lib/asteroids');

function collisionDetection(asteroids){
  // for(var i = 0; i < 5; i++ ) {
  //   // for(y = 0; y < asteroids.length; y++) {
  //     var a = asteroids[i];
  //     if(ship.x > a.x && ship.x < a.x + 40 && ship.y > a.y && ship.y < a.y + 40) {
  //           console.log("collision detected!");
  //      }
  //   // }
  // }
}

module.exports = collisionDetection;
